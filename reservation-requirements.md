# HAL Cinema 予約機能 要件定義書（ドラフト）

## 1. 目的

HAL Cinema の上映スケジュールに対して、会員が座席とチケット種別を選択し、決済を行い、予約番号またはQRコードで鑑賞できる予約機能を実装する。

本書では、既存のDB設計を前提に、予約機能の実装に必要な業務要件、画面/API要件、DB設計上の不足、未決定事項を整理する。

---

## 2. 対象範囲

### 対象に含める

- 上映スケジュールの一覧・詳細表示
- 座席表の表示
- 空席・予約済み・選択中・仮押さえ中の判定
- 座席選択
- チケット種別選択
- 合計金額計算
- 予約作成
- 決済処理
- 予約完了
- 予約履歴表示
- 予約詳細表示
- QRコードまたは予約番号の発行
- キャンセル処理
- 管理者による上映スケジュール・座席・料金の管理

### 今回の初期実装から外してもよい

- 隣席リクエスト機能
- 推しクリエイター通知
- 鑑賞前リマインダー通知
- 外部決済サービスの本番連携
- ポイント・クーポン・割引
- 複数館対応

---

## 3. 前提

- HAL Cinema は1館のみ。
- スクリーンは複数存在する。
- スクリーンごとに座席構成が異なる。
- 予約はログイン済み会員・未ログインのゲストのどちらでも可能。
- ゲスト予約では、予約者名・メールアドレス・電話番号を入力する。
- ゲスト予約は、予約番号と専用照会トークンで予約詳細を確認できる。
- ゲスト予約の専用照会トークンは予約完了時に発行し、メールで送付する。
- 1回の予約で複数座席を購入できる。
- 1座席につき1つのチケット種別を選択する。
- 料金は予約時点の金額を保存する。
- 決済完了後に予約確定とする。

---

## 4. 予約フロー

1. ユーザーが映画または上映スケジュールを選択する。
2. システムが対象上映の座席表を表示する。
3. ユーザーが空席から座席を選択する。
4. ユーザーが各座席にチケット種別を割り当てる。
5. システムが合計金額を表示する。
6. 未ログインの場合、ユーザーが予約者情報を入力する。
7. ユーザーが決済方法を選択する。
8. システムが座席を仮押さえする。
9. 決済が成功した場合、予約を確定する。
10. 決済が失敗または期限切れの場合、仮押さえを解除する。
11. ゲスト予約の場合、システムが専用照会トークンを発行し、ハッシュ化して保存する。
12. ゲスト予約の場合、システムが専用照会トークンを予約者メールアドレスへ送付する。
13. 予約完了画面に予約番号・QRコードを表示する。

---

## 5. 機能要件

### 5.1 上映スケジュール表示

- ユーザーは映画別に上映日時を確認できる。
- ユーザーは日付別に上映スケジュールを確認できる。
- 表示対象は `screenings.status = scheduled` の上映のみとする。
- 上映開始済み、終了済み、キャンセル済みの上映は予約不可とする。
- 上映ごとに以下を表示する。
  - 映画タイトル
  - 上映開始時刻
  - 上映終了時刻
  - スクリーン番号
  - スクリーンコンセプト
  - 字幕・吹替・原語
  - 残席数または販売状況

### 5.2 座席表表示

- ユーザーは上映回ごとの座席表を確認できる。
- 座席状態は以下を区別して表示する。
  - 空席
  - 選択中
  - 予約済み
  - 仮押さえ中
  - 車椅子対応席
- 同じ上映回で既に予約済みの座席は選択できない。
- 仮押さえ中の座席は選択できない。
- 車椅子対応席は通常席と見た目を分ける。

### 5.3 座席選択

- ユーザーは1回の予約で1席以上を選択する。
- 1回の予約で選択できる最大座席数を設定する。
- 初期案では最大6席とする。
- 選択した座席は決済開始時に仮押さえする。
- 仮押さえの有効期限を設定する。
- 初期案では仮押さえ期限を10分とする。

### 5.4 チケット種別選択

- ユーザーは座席ごとにチケット種別を選択する。
- 選択可能なチケット種別は `ticket_types.is_active = true` のみとする。
- 予約時点の料金を `booking_seats.unit_price` に保存する。
- チケット種別の料金が後で変更されても、過去予約の金額は変わらない。

### 5.5 予約作成

- 予約作成時に予約番号を発行する。
- 予約番号は一意で推測しにくい値にする。
- 会員予約の場合は `bookings.user_id` に会員IDを保存する。
- ゲスト予約の場合は `bookings.user_id` をNULLにし、予約者情報を予約に保存する。
- ゲスト予約では予約完了後に専用照会トークンを発行する。
- 専用照会トークンは平文保存せず、ハッシュ化して `bookings.guest_lookup_token_hash` に保存する。
- 専用照会トークンは予約者メールアドレスへ送付する。
- 予約完了画面では、予約番号と「照会トークンをメール送付したこと」を表示する。
- 予約作成時点では決済待ち状態を持つ。
- 決済成功後に予約確定状態にする。
- 決済失敗または仮押さえ期限切れの場合は予約を期限切れまたはキャンセル扱いにする。

### 5.6 決済

- 初期実装では決済方法として以下を扱う。
  - クレジットカード
  - QR決済
- 決済ステータスは以下を扱う。
  - 未決済
  - 決済処理中
  - 決済完了
  - 決済失敗
  - 返金済み
- 外部決済サービスと連携する場合、決済取引IDを保存する。
- 決済完了前に座席を他ユーザーへ販売してはならない。

### 5.7 予約完了

- 予約完了画面に以下を表示する。
  - 予約番号
  - QRコード
  - 映画タイトル
  - 上映日時
  - スクリーン
  - 座席
  - チケット種別
  - 合計金額
  - 決済方法
- 会員予約の場合、予約完了後にマイページの予約履歴へ表示する。
- ゲスト予約の場合、予約完了後に予約番号と専用照会トークンを使って予約詳細へアクセスできる。

### 5.8 予約履歴・予約詳細

- 会員は自分の予約履歴を確認できる。
- 会員は自分の予約詳細を確認できる。
- ゲストは予約番号と専用照会トークンを入力して予約詳細を確認できる。
- 他ユーザーまたは他ゲストの予約は閲覧できない。
- 会員の予約履歴では以下を表示する。
  - 予約番号
  - 映画タイトル
  - 上映日時
  - ステータス
  - 合計金額

### 5.9 キャンセル

- ユーザーは条件を満たす予約をキャンセルできる。
- 初期案では上映開始30分前までキャンセル可能とする。
- 決済済み予約をキャンセルした場合、返金処理を行う。
- キャンセル済みの座席は再販売可能にする。
- 上映開始後のキャンセルは不可とする。

### 5.10 管理者機能

- 管理者は映画、スクリーン、座席、上映スケジュール、チケット種別を管理できる。
- 管理者は上映スケジュールをキャンセルできる。
- 上映がキャンセルされた場合、該当予約をキャンセルまたは返金対象にする。
- スクリーン内で上映時間が重複するスケジュールは登録できない。

---

## 6. 非機能要件

### 6.1 同時予約対策

- 同じ上映回の同じ座席が二重予約されないこと。
- 座席確保処理はトランザクション内で実行する。
- DB制約でも二重予約を防ぐ。
- アプリケーション層だけに依存しない。

### 6.2 性能

- 座席表表示は通常の操作で待たされない速度にする。
- 上映回ごとの予約済み座席一覧を効率よく取得できるインデックスを用意する。

### 6.3 セキュリティ

- 会員の予約履歴取得には認証を必須とする。
- ゲスト予約の詳細取得には、予約番号と専用照会トークンを必須とする。
- 予約詳細は予約者本人または正しい照会情報を持つゲストのみ閲覧可能とする。
- 予約番号は推測困難な値にする。
- ゲスト予約の専用照会トークンは推測困難なランダム値にする。
- ゲスト予約の専用照会トークンは平文保存しない。
- ゲスト予約照会APIはレート制限を行う。
- ゲスト予約照会APIは、予約番号またはトークンのどちらが間違っているかをレスポンスで区別しない。
- 決済関連情報は必要最小限のみ保存する。
- クレジットカード番号は自社DBに保存しない。

### 6.4 監査性

- 予約作成日時、キャンセル日時、決済日時を追跡できる。
- 決済失敗や返金の状態を確認できる。
- 管理者による上映キャンセルを追跡できる。

---

## 7. 既存DB設計で使えるテーブル

| テーブル | 予約機能での用途 |
|---|---|
| `users` | 会員予約の予約者 |
| `movies` | 予約対象の映画 |
| `theaters` | 館情報 |
| `screens` | 上映スクリーン |
| `seats` | 座席マスタ |
| `screenings` | 上映回 |
| `ticket_types` | チケット種別・料金 |
| `bookings` | 予約ヘッダ |
| `booking_seats` | 予約座席明細 |

---

## 8. DB設計上の不足・修正候補

### 8.1 同じ上映回での座席二重予約をDB制約で防げない

現状の `booking_seats` は `(booking_id, seat_id)` のUNIQUE制約のみであり、別予約で同じ上映回・同じ座席を予約できてしまう可能性がある。

#### 対応案

現在有効な座席確保を表す専用テーブル `screening_seat_locks` を追加する。

このテーブルは「仮押さえ中」と「予約確定済み」の座席だけを保持する。キャンセル、決済失敗、仮押さえ期限切れの場合はレコードを削除し、座席を再販売可能にする。

#### 追加候補: `screening_seat_locks`

| カラム名 | 型 | 制約 | 説明 |
|---|---|---|---|
| `id` | BIGINT | PK, AUTO_INCREMENT | |
| `screening_id` | BIGINT | FK → screenings.id, NOT NULL | 上映回 |
| `seat_id` | BIGINT | FK → seats.id, NOT NULL | 座席 |
| `user_id` | BIGINT | FK → users.id | 会員予約の場合の確保ユーザー |
| `guest_token_hash` | VARCHAR(255) | | ゲスト予約の仮押さえ照合用 |
| `booking_id` | BIGINT | FK → bookings.id | 予約確定後に紐付く予約 |
| `status` | ENUM('held','confirmed') | NOT NULL | 仮押さえ中 / 確定済み |
| `expires_at` | DATETIME | | 仮押さえ期限。確定後はNULL |
| `created_at` | DATETIME | NOT NULL | |
| `updated_at` | DATETIME | NOT NULL | |

制約候補:

```sql
UNIQUE (screening_id, seat_id)
```

この制約により、同じ上映回の同じ座席は、仮押さえ中または確定済みのどちらであっても1件しか存在できない。

### 8.2 仮押さえを表現する仕組みがない

決済中に座席を一時的に確保する仕組みが必要。

#### 対応案

- `screening_seat_locks.status = held` で仮押さえを表す。
- `expires_at` を過ぎた仮押さえは無効とする。
- 期限切れの仮押さえは定期処理または予約処理時に削除する。
- 決済成功時に `status = confirmed` へ更新し、`booking_id` を紐付ける。

### 8.3 予約ステータスが不足している

現状の `bookings.status` は `confirmed` / `cancelled` のみで、決済待ち・期限切れを表現しにくい。

#### 修正候補

`bookings.status` を以下にする。

- `pending`
- `confirmed`
- `cancelled`
- `expired`

### 8.4 ゲスト予約の予約者情報を保存できない

現状の `bookings.user_id` は `NOT NULL` のため、未ログインのゲスト予約を表現できない。また、予約者名・連絡先・専用照会トークンを保存する場所がない。

#### 修正候補

`bookings.user_id` をNULL許可に変更し、以下を追加する。

| カラム名 | 型 | 制約 | 説明 |
|---|---|---|---|
| `booking_type` | ENUM('member','guest') | NOT NULL | 会員予約 / ゲスト予約 |
| `guest_name` | VARCHAR(100) | | ゲスト予約者名 |
| `guest_email` | VARCHAR(255) | | ゲスト連絡先メール |
| `guest_phone` | VARCHAR(30) | | ゲスト連絡先電話番号 |
| `guest_lookup_token_hash` | VARCHAR(255) | | 予約照会用トークンのハッシュ |
| `guest_lookup_token_expires_at` | DATETIME | | 予約照会用トークンの有効期限 |
| `guest_lookup_token_sent_at` | DATETIME | | 予約照会用トークン送付日時 |

制約候補:

- `booking_type = member` の場合、`user_id` は必須。
- `booking_type = guest` の場合、`guest_name`、`guest_email`、`guest_lookup_token_hash` は必須。
- ゲスト予約でも予約番号は必須。

#### 専用照会トークン仕様

- トークンは暗号学的に安全な乱数で生成する。
- トークンは十分な長さを持つURL-safeな文字列にする。
- トークンの平文はメール送付時のみ使用し、DBには保存しない。
- DBにはハッシュ化した値のみ保存する。
- トークンの有効期限は上映終了後24時間までとする。
- トークン期限切れ後に予約詳細が必要な場合、予約番号とメールアドレスを入力して再送信できる。
- 再送信時は新しいトークンを発行し、既存のトークンハッシュを無効化する。

### 8.5 決済ステータスが不足している

現状の `payment_status` は `pending` / `completed` / `refunded` のみで、決済失敗や決済処理中を表現しにくい。

#### 修正候補

`bookings.payment_status` を以下にする。

- `unpaid`
- `processing`
- `completed`
- `failed`
- `refunded`

### 8.6 決済取引情報を保存する場所がない

外部決済と連携する場合、決済取引ID、決済日時、返金日時、失敗理由などが必要。

#### 追加候補: `payments`

| カラム名 | 型 | 制約 | 説明 |
|---|---|---|---|
| `id` | BIGINT | PK, AUTO_INCREMENT | |
| `booking_id` | BIGINT | FK → bookings.id, NOT NULL | |
| `provider` | VARCHAR(50) | NOT NULL | 決済事業者 |
| `provider_transaction_id` | VARCHAR(100) | UNIQUE | 外部決済ID |
| `method` | ENUM('credit_card','qr') | NOT NULL | |
| `amount` | INT | NOT NULL | |
| `status` | ENUM('processing','completed','failed','refunded') | NOT NULL | |
| `paid_at` | DATETIME | | |
| `refunded_at` | DATETIME | | |
| `failure_reason` | VARCHAR(255) | | |
| `created_at` | DATETIME | NOT NULL | |
| `updated_at` | DATETIME | NOT NULL | |

初期実装で外部決済をモックにする場合でも、将来連携を考えるなら追加推奨。

### 8.7 キャンセル日時・返金日時がない

予約キャンセルの監査・表示のために日時が必要。

#### 追加候補

`bookings` に以下を追加する。

- `cancelled_at`
- `updated_at`

### 8.8 QRコード検証用の状態が不足している

入場済みかどうかを管理する場合、チェックイン状態が必要。

#### 追加候補

初期実装で入場確認まで行う場合、`bookings` に以下を追加する。

- `checked_in_at`

または `check_ins` テーブルを追加する。

### 8.9 上映スケジュール重複チェックに必要な制約がない

同じスクリーンで上映時間が重複しないようにする必要がある。

#### 対応案

- アプリケーション層で `screen_id`、`start_time`、`end_time` の重複をチェックする。
- DBがPostgreSQLの場合は排他制約も検討する。

### 8.10 管理者ユーザーの定義がない

管理画面を作る場合、管理者権限が必要。

#### 対応案

`users` に `role` を追加する。

- `member`
- `admin`

---

## 9. 推奨DB追加・変更まとめ

### 追加推奨テーブル

| テーブル | 理由 | 優先度 |
|---|---|---|
| `screening_seat_locks` | 仮押さえ・確定済み座席の一意管理 | 高 |
| `payments` | 決済取引・返金・失敗理由の管理 | 中 |

### 変更推奨カラム

| テーブル | 変更内容 | 優先度 |
|---|---|---|
| `bookings` | `status` に `pending` / `expired` を追加 | 高 |
| `bookings` | `payment_status` に `processing` / `failed` を追加 | 高 |
| `bookings` | `cancelled_at`, `updated_at` を追加 | 高 |
| `bookings` | `user_id` をNULL許可に変更 | 高 |
| `bookings` | ゲスト予約用の予約者情報・照会トークンを追加 | 高 |
| `bookings` | `guest_lookup_token_expires_at`, `guest_lookup_token_sent_at` を追加 | 高 |
| `users` | 管理画面が必要なら `role` を追加 | 中 |

### 追加検討カラム

| テーブル | カラム | 理由 |
|---|---|---|
| `bookings` | `checked_in_at` | QR入場確認 |
| `bookings` | `expires_at` | 決済待ち予約の期限 |
| `booking_seats` | `screening_id` | 予約座席取得の高速化 |

---

## 10. API要件案

### ユーザー向け

| メソッド | パス | 用途 |
|---|---|---|
| GET | `/api/screenings` | 上映スケジュール一覧 |
| GET | `/api/screenings/{id}` | 上映詳細 |
| GET | `/api/screenings/{id}/seats` | 座席表・座席状態取得 |
| POST | `/api/seat-locks` | 座席仮押さえ |
| DELETE | `/api/seat-locks/{id}` | 仮押さえ解除 |
| GET | `/api/ticket-types` | チケット種別一覧 |
| POST | `/api/bookings` | 予約作成 |
| POST | `/api/bookings/{id}/payments` | 決済開始・確定 |
| GET | `/api/bookings` | 自分の予約履歴 |
| GET | `/api/bookings/{id}` | 自分の予約詳細 |
| POST | `/api/guest-bookings/lookup` | ゲスト予約照会 |
| POST | `/api/guest-bookings/resend-token` | ゲスト予約照会トークン再送 |
| POST | `/api/bookings/{id}/cancel` | 予約キャンセル |

### 管理者向け

| メソッド | パス | 用途 |
|---|---|---|
| POST | `/api/admin/screenings` | 上映スケジュール作成 |
| PATCH | `/api/admin/screenings/{id}` | 上映スケジュール更新 |
| POST | `/api/admin/screenings/{id}/cancel` | 上映キャンセル |
| POST | `/api/admin/ticket-types` | チケット種別作成 |
| PATCH | `/api/admin/ticket-types/{id}` | チケット種別更新 |
| GET | `/api/admin/bookings` | 予約一覧 |

---

## 11. 画面要件案

### ユーザー画面

- 上映スケジュール一覧画面
- 映画詳細画面
- 座席選択画面
- チケット種別選択画面
- 予約内容確認画面
- 決済画面
- 予約完了画面
- マイページ予約履歴
- 予約詳細画面
- ゲスト予約照会画面

### 管理者画面

- 上映スケジュール管理
- 上映スケジュール登録・編集
- 予約一覧
- チケット種別管理
- 座席管理

---

## 12. 未決定事項

以下は実装前に決める必要がある。

| 論点 | 初期案 |
|---|---|
| 1回の予約で選択できる最大座席数 | 6席 |
| 仮押さえ期限 | 10分 |
| キャンセル期限 | 上映開始30分前まで |
| 返金手数料 | 初期実装では無料 |
| ゲスト予約 | 初期実装から可能 |
| ゲスト予約の照会方法 | 予約番号 + 専用照会トークン |
| ゲスト照会トークンの有効期限 | 上映終了後24時間まで |
| 決済方式 | 初期実装はモック、後で外部決済連携 |
| QRコード入場確認 | 初期実装では予約詳細表示まで。入場確認は後続 |
| 車椅子席の予約制限 | 初期実装では誰でも選択可能。注意表示のみ |
| 座席の連番必須 | 初期実装では任意 |
| 上映開始何分前まで購入可能か | 上映開始10分前まで |
| 管理者機能を同時に作るか | 最低限の上映・料金管理は必要 |

---

## 13. 実装優先度

### Phase 1: 予約の最小機能

- 上映スケジュール表示
- 座席表表示
- 座席選択
- チケット種別選択
- 予約作成
- 決済モック
- 予約完了
- 予約履歴
- ゲスト予約照会
- 二重予約防止

### Phase 2: 予約運用に必要な機能

- 仮押さえ期限切れ処理
- キャンセル・返金
- 管理者の上映スケジュール管理
- 管理者の料金管理
- 上映キャンセル時の予約処理

### Phase 3: 追加価値機能

- QR入場確認
- 鑑賞前リマインダー
- 推し通知
- 隣席リクエスト
- 外部決済本番連携

---

## 14. 受け入れ条件

- 同じ上映回の同じ座席を複数ユーザーが同時に予約しようとしても、確定できるのは1人だけである。
- 決済中の座席は他ユーザーから選択できない。
- 仮押さえ期限が切れた座席は再び選択できる。
- 決済成功後、予約番号が発行される。
- 会員予約の完了後、ユーザーはマイページで予約を確認できる。
- ゲスト予約の完了後、予約番号と専用照会トークンで予約詳細を確認できる。
- ゲスト予約の専用照会トークンはDBに平文保存されない。
- ゲスト予約の専用照会トークン期限切れ後、新しいトークンを再送できる。
- 予約者本人、または正しい照会情報を持つゲスト以外は予約詳細を閲覧できない。
- キャンセル可能期限内であれば予約をキャンセルできる。
- キャンセルされた座席は再販売できる。
- 上映キャンセル時に対象予約を抽出できる。
- チケット料金変更後も過去予約の金額は変わらない。

---

## 15. 次に決めること

1. 初期実装に外部決済を含めるか、モック決済にするか。
2. `screening_seat_locks` テーブルを追加するか。
3. 予約済み座席の一意性をどのテーブルで担保するか。
4. キャンセル・返金をPhase 1に含めるか。
5. 管理者画面をどこまで初期実装に含めるか。
6. QRコードは表示のみか、入場チェックまで実装するか。
7. ゲスト予約照会トークンの再送時に、追加で電話番号確認を求めるか。
